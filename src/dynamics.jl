function initialize_dynamics!(ps::PowerSystem, psol::PowerFlowSolution)
    # Create dynamic load models (by default, constant admittance)

    # Initialize dynamic models and assemble z0, p.
    
    # Initialize Jacobian matrix structure.
end
